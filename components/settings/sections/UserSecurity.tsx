import React from 'react';
import PlaceholderSection from './PlaceholderSection';

const UserSecurity: React.FC = () => {
  return (
    <PlaceholderSection 
      title="User & Security Settings" 
      description="Manage user accounts, permissions, and security settings including two-factor authentication."
    />
  );
};

export default UserSecurity;
